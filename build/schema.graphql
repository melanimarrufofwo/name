type TransferCounter @entity {
  id: ID!
  count: Int!
}

type UserCounter @entity {
  id: ID!
  count: Int!
}

type User @entity {
  id: ID!
  address: String!
  balance: BigInt!
  transactionCount: Int!
  burnCount: Int!
  burnAmount: BigInt!
  approvalCount: Int!
  approvalAmount: BigInt!
}
